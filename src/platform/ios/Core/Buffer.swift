//
//  Buffer.swift
//  airhockey
//
//  Created by Sprite on 2018/12/1.
//  Copyright © 2018年 Learn OpenGL ES. All rights reserved.
//

import Foundation
import GLKit

//func createVBO(size:Int, data:GLvoid, usage:GLenum) -> GLuint {
//    assert(data != NULL);
//    GLuint vbo_object;
//    glGenBuffers(1, &vbo_object);
//    assert(vbo_object != 0);
//
//    // The size, in memory, of a Vertex structure.
//    let vertexSize = MemoryLayout<Vertex>.stride
//    // Generatea a buffer for our vertex buffer object.
//    glGenBuffers(1, &vbo_object)
//    // Bind the vertex buffer object we just generated (created).
//    glBindBuffer(GLenum(GL_ARRAY_BUFFER), vbo_object)
//    // Pass data for our vertices to the vertex buffer object.
//    //        let vertexBufferSize = Vertices.count * vertexSize
//    glBufferData(GLenum(GL_ARRAY_BUFFER), vertexBufferSize, data, usage)
//
//    return vbo_object;
//}

